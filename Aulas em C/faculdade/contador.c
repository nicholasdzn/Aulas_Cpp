#include <stdio.h>

int main(){
    int contador = 0;
    
    contador ++; // contador = contador + 1

    printf("contador %d \n", contador);

    contador += 2; 

    printf("contador %d \n", contador);
}