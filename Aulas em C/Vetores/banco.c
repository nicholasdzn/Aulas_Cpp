#include <stdio.h>
#define clientes 10

void banco(float saldo[],int conta[]){
    int i = 0;

    for(i = 0; i<= 200; i++)
    {
    printf("Digite o numero da conta do cliente %d", i+1);
        scanf("%d", &conta[i]);
    printf("Digite o saldo do cliente %d", i+1);
        scanf("%d", &saldo[i]);
    if(conta = 0){
        break;
    }
    }
    
}

float medias(float saldo[], int num){
    float somas = 0;
    int i;

    for(i=0; i<num; i++){
        
    }
}


